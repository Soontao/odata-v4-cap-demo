{
  "name": "cap-demo",
  "version": "1.0.0",
  "private": "true",
  "engines": {
    "node": "16",
    "npm": "8"
  },
  "dependencies": {
    "@sap/cds": "^5.9.2",
    "cds-mysql": "^5.9.9-alpha.0",
    "express": "^4.17.3"
  },
  "scripts": {
    "start": "npx cds run",
    "deploy": "cds-mysql-deploy",
    "test": "jest --silent --forceExit"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "mysql"
      },
      "mysql": {
        "impl": "cds-mysql"
      }
    }
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000,
    "testMatch": [
      "**/*.test.js"
    ]
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "axios": "^0.22.0",
    "cds-jest": "^0.0.6",
    "chai": "^4.3.6",
    "chai-as-promised": "^7.1.1",
    "chai-subset": "^1.6.0",
    "dotenv": "^10.0.0",
    "jest": "^27.5.1"
  }
}