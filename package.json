{
  "name": "odata-v4-demo",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@sap/cds": "^5",
    "express": "^4",
    "sqlite3": "^5",
    "@sap/cds-dk": "^4.8.2"
  },
  "scripts": {
    "start": "cds deploy --to sqlite && cds run"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "sqlite.db"
        }
      }
    }
  }
}