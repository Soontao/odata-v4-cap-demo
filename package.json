{
  "name": "odata-v4-demo",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@sap/cds": "^5",
    "express": "^4",
    "sqlite3": "^5"
  },
  "scripts": {
    "start": "npx cds deploy && npx cds run"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      }
    }
  }
}