{
  "name": "odata-v4-demo",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "@sap/cds": "^5",
    "cds-mysql": "^5.3.0",
    "dotenv": "^10.0.0",
    "express": "^4",
    "sqlite3": "^5"
  },
  "scripts": {
    "start": "npx cds run",
    "deploy": "cds-mysql-deploy"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "mysql"
      },
      "mysql": {
        "impl": "cds-mysql",
        "models": [
          "srv",
          "db"
        ]
      }
    }
  }
}